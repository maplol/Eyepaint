# EYEPAINT

Приложение в духе Painter’s Eye: загружаешь референс, наводишь камеру на лист, выставляешь прозрачность и рисуешь поверх — как через кальку.

## Как пользоваться

1. Открой приложение на телефоне (удобнее всего).
2. **Сфотографируй** референс или возьми **из галереи**.
3. Разреши доступ к камере — лучше задней.
4. Подгони картинку: перетаскивание, щипок (масштаб/поворот), ползунок прозрачности.
5. Кнопка затвора сохраняет на телефон **композит**: камера + референс.
6. Таб **Цвета**: кружки палитры, серый фон / «Только цвет» (с рамкой).
7. Таб **Позы**: сохранить позицию оверлея и вернуть позже.
8. **Скрыть UI** убирает панели; прозрачная кнопка в правом нижнем углу возвращает их.

## Запуск

```bash
npm install
npm run dev
```

Камера работает только в безопасном контексте: `localhost` или HTTPS.

## Сборка

```bash
npm run build
npm run preview
```

## Телефон / GitHub Pages

После мержа в `main` GitHub Action деплоит сайт сюда:

**https://maplol.github.io/Eyepaint/**

Один раз в репозитории: **Settings → Pages → Build and deployment → Source: GitHub Actions**.
